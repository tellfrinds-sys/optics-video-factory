# ذاكرة مشروع أكاديمية بوابة البصريات — النسخة التفصيلية الكاملة

> **لأي جلسة Claude جديدة تقرأ الملف ده:** ده نقل تفصيلي كامل (مش تلخيص) لكل ذاكرتي المتراكمة عن مشروع «أكاديمية بوابة البصريات». اقرأه كامل قبل أي تنفيذ. لا تعتبر أي تفصيلة هنا "حالة حيّة مؤكّدة" — تحقّق من الكود الفعلي على السيرفر قبل الاعتماد على أي سطر كحقيقة تشغيلية دقيقة، خصوصًا القديم منه.
>
> **صفحة مرجعية سريعة (ملخّص + حالة اليوم):** https://claude.ai/code/artifact/f86969fc-a6c0-47c8-b215-314ed37e6e8e
>
> آخر تحديث لهذا الملف: **١٣ سبتمبر ٢٠٢٦**. مفاتيح API الفعلية **لا تُذكر هنا أبدًا** حتى لو ظهرت في نقاش سابق — هي فقط في `/etc/systemd/system/videofactory.service` على السيرفر.

---

## القسم ١ — نظرة عامة وبنية الوصول (أحدث حالة)

**مشروع مصنع الفيديوهات التعليمية «بوابة البصريات»** — يولّد فيديوهات درس تلقائيًا عبر n8n + سيرفر رندر Python، بصوت مؤسّس الأكاديمية الحقيقي.

- **SSH**: `~/.ssh/optics_factory` (بلا passphrase) → `root@31.97.53.155` (Hostinger VPS srv1925952، Ubuntu 24.04). **بديل بدون مفتاح**: Hostinger hPanel → VPS → Web Console (من أي متصفح).
- نفس السيرفر يشغّل: n8n (pm2، منفذ محلي **5678**)، `videofactory.service` (منفذ 8000)، Ollama، nginx.
- **الدومين `opticsgate.online`**: النيم سيرفرز **يجب أن تكون بالظبط** اللي VPS DNS Manager (Hostinger) بيعيّنها (`ns1/ns2.dns-parking.com`) — أي تغيير عنها بيقطع الموقع بصمت لأيام (حصل فعليًا في ١١-١٣ سبتمبر، السبب الجذري كان تبديل النيم سيرفرز لغير القيمة المعيَّنة من VPS Manager تحديدًا).
- **Supabase**: مشروع `optics Gate-core` (`ilvgahgnaxxooxvcawyh`). Google One للتخزين: `opticsgate@gmail.com` (خطة 5TB).
- **GitHub**:
  - `tellfrinds-sys/optics-video-factory` (كود المصنع، deploy key `github_deploy_repo3`) — نسخة احتياطية **تلقائية يوميًا** (`/root/video-factory/auto_backup.sh` + cron `0 3 * * *`) + مرآة إضافية على `opticsgate-rgb/opticsgate-cloud-backup` عبر GitHub Action (`backup-mirror.yml`).
  - `tellfrinds-sys/opticsgate-website` (كود الموقع، deploy key `github_deploy` أو `gh_website_deploy`).
- **مفاتيح الخدمات** كلها Environment= في `/etc/systemd/system/videofactory.service`: Cartesia، Gemini، Google Cloud TTS، HeyGen، Supabase. **لا تُنسخ هذه القيم في أي ملف Markdown أو مستند سحابي أو GitHub — فقط تُذكر أماكن وجودها.**

---

## القسم ٢ — سيرفر الرندر `/root/video-factory/`

- `narrated_render.py` = المحرّك الرئيسي. `main.py` = خادم HTTP (منفذ 8000، systemd `videofactory.service`).
- `corrected_scenes.py` / `eye_scene2.py` = رسم مشاهد العين (صور Wikimedia واقعية + مسميات). `eye_scene2` يدعم `diagram="cornea_layers"` (epithelium/bowman/stroma/descemet/endothelium/tearfilm).
- **بوابة المراجعة النهائية**: `qa_gate.gemini_audio_review` — يرسل الصوت الناتج + السيناريو + الهدف لـ Gemini، يراجع اللهجة/مخارج الحروف/التوقفات/المطابقة/التغطية. حلّ محل faster-whisper+qwen2.5:7b القديم.
- **وكيل الإصلاح الذاتي**: `agents.auto_remediate(qa, fs, lesson)` — لو فشلت المراجعة، Gemini يحلّل ويصلح ما يُصلَح تلقائيًا (`script_fixes`)، يتعلّم قاعدة (`bump_style_guide`)، يعيد الرندر. `produce_lesson.produce()` بيصدّر دايمًا في النهاية (مفيش `qa_failed` نهائي بلا محاولة إصلاح).
- **التصدير**: rclone remote `optics_drive` (`opticsgate@gmail.com`) → مجلد `OpticsGate/Videos/`. ⚠️ rclone بيستخدم client_id مشترك هيتوقف خلال 2026 — يحتاج client_id خاص لاحقًا.
- **أعمدة `lessons` في Supabase**: `video_url`, `video_backup_url`, `video_duration_seconds`, `video_qa_verdict`, `video_published_at`, `video_status` (draft|qa_failed|ready|published), `objectives_ar`, `question_bank_ar`, `scientific_refs_ar`, `primary_audience_uid` (انظر القسم ٥).
- **حذف تلقائي للنسخ القديمة**: `produce_lesson._purge_old_versions(code)` يمسح فيديوهات نفس رمز الدرس من الأرشيف/Drive/Supabase Storage قبل رفع نسخة جديدة.
- **بعد أي `scp` لملف تستورده `videofactory`**: لازم `systemctl restart videofactory` (Python بيكاش الموديول). أي تشغيل يدوي عبر سكربت SSH منفصل (مش عبر systemd) **لازم يصدّر نفس متغيّرات البيئة يدويًا وإلا هيرجع للقيم الافتراضية الخاطئة** (حصل: إنتاج درس استخدم ElevenLabs بالخطأ بدل Cartesia لأن السكربت اليدوي ماكانش وارث `TTS_BACKEND=cartesia`).

---

## القسم ٣ — خط إنتاج الدرس الكامل (Pipeline E2E)

```
/api/prepare-lesson {lesson_uid}
  → agents.write_script (Gemini + prm_writer.txt) + agents.review_script (مراجعة تحريرية + dialect_lint، حتى 3 محاولات)
  → حفظ SCRIPT_FINAL (pending) في content_outputs
  → webhook send-script-card عبر http://127.0.0.1:5678 (محلي إجباري — الدومين العام لـn8n قد ينقطع بصمت)
  → بطاقة تليجرام: ✅ اعتماد وإنتاج | ✏️ نسخة معدّلة (يلصق المستخدم 〔مشهد N〕...) | 🔄 إعادة توليد
  → produce_lesson.produce_async (subprocess منفصل — يجب أن يرث بيئة الخدمة كاملة)
  → narrated_render (رسم + Cartesia TTS) → qa_gate (مراجعة Gemini سمعية) → حلقة إصلاح ذاتي عند الفشل
  → رفع Drive + Supabase Storage + أرشيف محلي → بطاقة فيديو تليجرام (vok/vedit/vredo)
  → vok:<lesson_uid> → webhook prepare-next → تجهيز الدرس التالي تلقائيًا (السلسلة تكمل لكل الـ355)
```

- `agents.parse_script_text(text, base)`: يدمج المشاهد الملصوقة يدويًا فوق `base["scenes"]` بمطابقة `scene_no` — يحتفظ بـ `diagram`/`term_en`/`labels`/`visual_focus` من الأصل لو المستخدم ما حدّدهاش.
- `render_script_text(fs)`: يبني الصيغة النصية القابلة للنسخ والتعديل (`〔مشهد N〕 عنوان | رسم: X | EN: Y`).
- `video_uid = 500128 + (lesson_uid - 100001)`.
- **لصق طويل عبر تليجرام**: بيتقسّم تلقائيًا لعدة رسائل — السيرفر بيجمّعهم بـ buffered debounce (16 ثانية) قبل المعالجة.

**قواعد وكلاء الكتابة والمراجعة — محدَّثة ١٣ سبتمبر ٢٠٢٦ (`pipeline/prm_writer.txt` / `pipeline/prm_reviewer.txt`):**
- **تشكيل شامل إجباري صفر-تسامح** على كل كلمة في `narration` (كان انتقائيًا قبل كده لتقليل الحجم — تغيّر القرار بطلب صريح من المستخدم). فتحة/ضمة/كسرة/سكون/شدة على كل حرف، بما فيها أواخر الكلمات.
- كلمات حرجة موثّقة: سُمك (لا سَمك)، القَرنِيّة بالقاف الصريحة (لا الهمزة أبدًا)، يُفَضَّل بضم الياء، أكسِچين/فِسيولوچيا (جيم معطَّشة چ).
- **أي تصحيح لغوي أو تشكيلي يُمنَع أن يغيّر المعنى العلمي أو الهدف التعليمي للجملة الأصلية.**
- المراجع لازم `verdict=fail` لو فيه أي `tashkeel_violations` (حقل جديد في مخرجات JSON).
- محاولات الكتابة/المراجعة: **3** (كانت 2).
- **الهدف المعلن من المستخدم**: تقليل الاعتماد البشري تدريجيًا بعد ثبات صفر أخطاء على ١٠-١٥ درس متتالي بالقواعد الجديدة — **قرار صريح لاحق منه، مش تلقائي أبدًا**.

**اللهجة**: مصرية عامية **حصرًا**، ممنوع الفصحى نهائيًا — المستخدم غضب سابقًا من تحويل تلقائي غير مطلوب للفصحى. الأرقام بالحروف لا أرقام. لا تُبطئ TTS بـ`speed` لضبط الإيقاع — استخدم فجوات/توقفات بدلًا من ذلك.

---

## القسم ٤ — قرار الصوت الكامل (نهائي — لا يُعاد فتحه بدون طلب صريح جديد)

بالترتيب الزمني الكامل، كل محاولة ولماذا فشلت أو نجحت:

1. **ElevenLabs** — نطق غير قابل للتحكم إطلاقًا (تجاهل التشكيل تمامًا، القرنية→الأرنية رغم كل محاولات SSML/تهجئة). أُثبت 4 مرات. **مرفوض نهائيًا.**
2. **Piper (ar_JO-kareem) + طبقة فونيمات مصرية يدوية** (`piper_tts.py`, `_egy()`) — لهجة أردنية أساسية، تحسّنت الفونيمات لكن النبرة فضلت أردنية.
3. **XTTS-v2** (zero-shot، `coqui-tts` في venv `/root/xttsenv`) — حكم المستخدم: **"بعيد عن صوتي تمامًا وليس مصريًا والصوت منخفض"**. **لا تقترحه تاني أبدًا.**
4. **Piper → RVC v2** (مُدرَّب على ٣.٧ دقيقة صوت المستخدم عبر Colab) — أقرب نبرة لحد الآن، لكن صدى مباق + لسه مش مصري + بطيء.
5. **Azure AI Speech (ar-EG)** — قرار مخطَّط له (SSML + قاموس نطق مخصّص)، **لكن اتحظر عند إنشاء الحساب**: "You're not eligible for an Azure account" (مشكلة معروفة مع حسابات Gmail/مصر، لا حل معروف).
6. **Piper مدرَّب ذاتيًا من الصفر (v1-v5)** — ٥ جولات تدريب كاملة على ٥٨ دقيقة صوت حقيقي للمستخدم (`mix19.mp3`+`mix38.mp3`):
   - v3: val_mos 3.448، ~80% دقة كلمات، مصري فعلًا لكن يتعثّر في الكلمات الدخيلة ونهايات بعض الكلمات.
   - v4: warmstart من v3، فلتر أخف، تشكيل mishkal كامل على نص التدريب.
   - v5: warmstart من أفضل نقطة v4، بيانات أنضف، سلاسة محسّنة (دمج مقاطع + مزج متساوي القدرة + سكتات طبيعية بدل فجوات رقمية حادة).
   - **الحكم النهائي على v5 (بعد رفع الصوت والإبطاء بطلب المستخدم): "تجربة فاشلة كليًا".** **مسار Piper الذاتي مُغلَق تمامًا — لا يُقترَح تاني.**
7. **F5-TTS** (`MAdel121/f5-tts-egyptian-arabic`, استنساخ صفري مجتمعي) — حكم المستخدم: "فشل تمامًا". كمان: ترخيص **CC-BY-NC-4.0 غير تجاري** (عائق قانوني وليس فقط جودة)، الموديل الأساسي 85% فصحى/13% مصري، UTMOS 2.63/5. السيرفر بلا GPU فشل بـOOM أول مرة (احتاج swapfile مؤقت اتشال بعدين) — 5 دقايق ونص لـ4.8 ثانية صوت فقط، تأكيد إن السيرفر ده مش مناسب لأي موديل من النوع ده.
8. **XTTS v2 / ChatTTS / Spark-TTS** (اقتُرحت كبدائل) — بحثت ولم أجرّب: XTTS مرفوض بالفعل + ترخيصه الآن غير-تجاري بحكم الواقع (Coqui أغلقت يناير 2024، محدّش يقدر يبيع ترخيص تجاري). ChatTTS/Spark-TTS نماذجهم الأساسية صينية/إنجليزية بس؛ أي نسخة عربية منها fine-tune مجتمعي غير رسمي وغالبًا برخصة غير تجارية. **نمط عام مؤكَّد: نماذج TTS مفتوحة المصدر البحثية (XTTS، F5-TTS، Fish Speech، ChatTTS، Spark-TTS) بتفشل بشكل منهجي مع العربية — إما بيانات تدريب عربية شبه معدومة، أو الفاين-تيون العربي المتاح غير مرخّص تجاريًا. لا تقترح المزيد من هذه الفئة.**
9. **Google Cloud Text-to-Speech (ar-XA، Chirp3-HD "Charon")** — **اعتُمد فعليًا لفترة قصيرة كإنتاج**: جودة نطق ممتازة، مستقر (منتج غير preview)، SSML/phoneme للتحكّم، Long Audio API للدفعات. التكلفة ~١.٨$/ساعة (Chirp3-HD) أو ٠.٩٦$/ساعة (Neural2 الأرخص). **لكنه مش صوت المؤسّس الحقيقي.**
10. **Cartesia (Sonic 3.6)** — المستخدم جهّز استنساخ حقيقي لصوته الخاص على cartesia.ai. اختُبر عبر نداء HTTP خام (`POST https://api.cartesia.ai/tts/bytes`، الهيدرز: `Content-Type`, `X-API-Key`, و**`Cartesia-Version` إجباري وإلا 400**). حكم المستخدم: **"أكثر من رائع"** — تم اختياره لأنه صوته الحقيقي، وليس رغم ذلك.

**⇒ القرار النهائي المعتمد للإنتاج: Cartesia، موديل sonic-3.6، صوت المؤسّس الحقيقي المستنسخ.**
- `cartesia_tts.py` على السيرفر: `cartesia_tts(text, dest, speed) -> None` (بدون توقيت كلمات — نفس واجهة باقي المحرّكات، fallback التوزيع النسبي في `narrated_render.py` يغطّي الترجمة المحروقة).
- `TTS_BACKEND=cartesia` في `videofactory.service`.
- Google Cloud TTS و`google_tts.py` باقيين على السيرفر كـfallback غير مُستخدَم (زي Piper قبله) — **لا تُفعَّل بدون طلب صريح.**
- **صوت الأفتار المتحرك (مقدمة/خاتمة كل درس) منفصل تمامًا عن صوت السرد**: الأفتار (HeyGen، أفتار مخصّص "Optics Gate1") محتاج مزامنة شفايف حقيقية مع الصوت، فمُقيَّد بأصوات HeyGen المستنسخة ("صوت نوح" ونسخته المحسّنة)، بينما Cartesia صوت مباشر بلا فيديو أفتار. **لا تلخبط بين "مقدمة/خاتمة الدرس الثابتة" (bookend يتكرر في كل الدروس) و"الفيديو الترويجي/التعريفي" (أصل تسويقي منفصل تمامًا، مش جزء من خط إنتاج الدروس)** — لخبطة حصلت فعليًا (استبدال ملف bookend الأصلي بالخطأ) واحتاجت تراجعًا كاملًا لحالته الأصلية.
- **HeyGen**: يستخدم فقط للأفتار المتحرك (مقدمة/خاتمة/فيديوهات ترويجية)، مش لسرد الدروس. رصيد الـ"api" credit محدود جدًا (فيديو أفتار ١٦ ثانية استهلك ٥٤ من ٦٠ كريدت) — احترس من الاستهلاك، اختبر بمقاطع قصيرة الأول. رصيد "plan_credit" منفصل (بيُستهلك من استوديو HeyGen اليدوي فقط، مش عبر API).

---

## القسم ٥ — بنية المنهج والمسارين

- **٣٥٥ درس، ٢٠ باب (B01-B20)، ٤٠ وحدة، ٨٠ فصل.** هرمية: `curriculum_doors` ← `curriculum_units` ← `curriculum_chapters` ← `lessons` (عبر `chapter_uid`).
- **عمود `primary_audience_uid` في `lessons`** يقسّم المحتوى:
  - `1` (211 درس) = محتوى ممارسين تأسيسي (تشريح، بصريات فيزيائية، عيوب انكسار، فحوصات، عدسات، إلخ).
  - `2` (98 درس) = محتوى ممارسين متخصّص (تقنيات الفحص، أجهزة B05 المسار المهني).
  - `4` (18 درس) = محتوى إداري/تشغيلي للممارسين (باب B20 — إدارة مركز البصريات).
  - `3` (28 درس، كلها حاليًا في باب B05) = محتوى جمهور مبسّط تثقيفي، بادئة العنوان "للجمهور |"، بلا مصطلحات مهنية.
  - **الإجمالي: 327 ممارسين + 28 جمهور = 355.**
- **صفحتان منفصلتان على الموقع**، كلاهما من `build_lessons_page.py` (نفس الدالة `_build_track` بمعامل فلترة):
  - `lessons.html` = مسار الممارسين (`primary_audience_uid != 3`).
  - `lessons-public.html` = مسار الجمهور (`primary_audience_uid = 3`).
  - شريط تنقّل ثابت في أعلى كل صفحة للتبديل بين المسارين.
- **باب B05** ("أجهزة البصريات والفحص والتصوير") فيه ازدواج مقصود: نفس الأجهزة (فوكيمتر، أوتورفراكتومتر، سليت لامب...) بنسختين — "المسار المهني المتخصص" (تقني عميق) و"للجمهور" (مبسّط) — مش تكرار خطأ، تصميم مقصود لجمهورين مختلفين لنفس الموضوع.
- **خطة معتمدة مستقبلًا**: بعد تقدّم كافٍ في إنتاج مسار الممارسين، تُضاف خطوة تلخيص تلقائي (Gemini) بعد اعتماد كل درس ممارس، تولّد نسخة جمهور مبسّطة لتغطية الـ327 درس تباعًا (حاليًا الجمهور يغطّي أجهزة B05 بس).
- **لا تحذف دروس/بيانات المنهج بدون رجوع للمستخدم صراحة** — الفلترة في العرض (query filter) بديل آمن ومفضّل دائمًا عن الحذف.

---

## القسم ٦ — دروس مستفادة من أعطال حقيقية (تجنّب التكرار)

1. **لا تُشغّل عمليات الإنتاج عبر سكربت SSH يدوي بدون تصدير متغيّرات بيئة الخدمة كاملة** — سبّب استخدام ElevenLabs (رصيده منتهٍ) بدل Cartesia، وفشل الإنتاج.
2. **أي اتصال داخلي بين خدمتين على نفس السيرفر يجب أن يستخدم `127.0.0.1` لا الدومين العام** — انقطاع الفرعي `n8n.opticsgate.online` بعد تغيير نيم سيرفرز الدومين سبّب فشل صامت لبطاقات تليجرام لأيام (الكود كان يبتلع الاستثناء بلا تسجيل — أُصلح: الآن يسجّل الخطأ في اللوج).
3. **الدومين: النيم سيرفرز يجب أن تطابق بالظبط ما يعيّنه VPS DNS Manager** — أي قيمة أخرى (حتى لو "افتراضية Hostinger") بتقطع الموقع.
4. **الملفات المُرسَلة كـ"بطاقة ملف" (file card) أحيانًا لا تفتح عند هذا المستخدم تحديدًا** — أرسل رابط استضافة مباشر (على السيرفر أو Artifact منشور) كبديل مضمون دائمًا، وأعد الإرسال كرابط لو اشتكى.
5. **قبل أي حذف من قاعدة بيانات الإنتاج (Supabase)**: افحص أعمدة التصنيف (زي `primary_audience_uid`) واقترح الفلترة بدل الحذف؛ **لا تحذف بيانات فعليًا إلا بموافقة صريحة موثّقة**.
6. **`git push` لمستودعات مرتبطة بموقع منشور (production) قد يُوقفه نظام حماية الجلسة الآلي** — لو حصل، اشرح للمستخدم بدل محاولة الالتفاف، واطلب إذنًا صريحًا للمتابعة.
7. **الكود الفعلي على السيرفر قد يتخلّف عن آخر backup لأسابيع لو محدش يعمل `git push` يدويًا** — الآن فيه `cron` يومي (`auto_backup.sh`) يمنع تكرار الفجوة دي.
8. **الملفات الضخمة (نماذج صوت مهجورة، تسجيلات صوت خام) يجب ألا تُرفع لـ GitHub** — `tts_models/`, `rvc_repo/`, `voice_src/` مُستبعَدة بـ`.gitignore` عمدًا (حجم + خصوصية).
9. **مفاتيح API الحيّة لا تُكتَب أبدًا داخل ملفات توثيق تُرفَع لـ GitHub أو أي مستند سحابي مشترك** — تبقى فقط في متغيّرات بيئة السيرفر.

---

## القسم ٧ — تفضيلات المستخدم الثابتة (تنطبق دائمًا)

- **مصري عامي فقط في كل سرد فيديو — ممنوع الفصحى نهائيًا.** التشكيل بسخاء على اللهجة المصرية (أفعال المضارع، المصادر، المصطلحات).
- **قدّم بدائل حقيقية (2-4 خيارات) مع مقايضاتها (تكلفة/جودة/جهد)، رشّح واحدًا مع السبب، واترك القرار له** — لا تفرض حلًا واحدًا إلا لو كان الوحيد فعليًا.
- **المستخدم غير مبرمج تقنيًا** — بسّط الشرح دائمًا، واطلب موافقته خطوة بخطوة قبل أي تعديل جوهري على الكود أو البيانات أو الأنظمة.
- **لا يريد الاعتماد على أي جهاز شخصي معيّن أبدًا** — سبب إنشاء صفحة "مركز القيادة" وهذا الملف؛ كل شيء جوهري يجب أن يكون سحابيًا ومتاحًا من أي جهاز/متصفح.
- **يريد تقليل الأخطاء المتكررة** — التوثيق الدائم هنا بديل عن الاعتماد على الذاكرة الاحتمالية وحدها.
- **المصطلحات الإنجليزية**: تُكتب عربيًا صوتيًا في narration (كورْنِيا، دَايُوبْتَر)، الأرقام بالحروف لا أرقام.

---

## القسم ٨ — خطة مؤجَّلة (تُستأنف عند شراء opticsgate.com)

- بنية مستهدفة: **نطاق واحد + نطاقات فرعية** (`app.` `api.` `admin.` `cdn.` `n8n.` `hooks.opticsgate.com`) بدل نطاقين منفصلين. نقل الـDNS لـCloudflare، شهادة wildcard واحدة.
- `opticsgate.online`: **لا يُلغى فورًا** — إعادة توجيه 301 لكل شيء إلى `.com`، الإبقاء عليه 6 أشهر (تأمين رخيص لأي روابط/QR مطبوعة)، ثم تركه ينتهي.
- ترقية VPS من 2 إلى 4 vCPU / 8-16GB رام — يسرّع النسخ/المعالجة الصوتية، لا يُفعِّل تدريب Piper/VITS (يحتاج GPU دائمًا → Colab). لا علاقة له بقرار الصوت الحالي (Cartesia سحابي بالكامل).
- **من يفعل ماذا**: المستخدم يشتري الدومين، يغيّر حجم الـVPS في لوحة Hostinger، يضيف سجلات DNS، يعيد التحقق من الدومين عند بوابة الدفع وقت الإطلاق. Claude: كل شيء على جانب السيرفر (nginx، شهادات SSL، إعادة التوجيهات، تحديث روابط الكود، إعادة تسجيل webhook تليجرام، ضبط الإعدادات، اختبار شامل + خطة تراجع).
- **كود يحتاج تحديث روابط `opticsgate.online`**: `build_lessons_page.py`, `produce_lesson.py`, `main.py`, `pipeline/prepare_lesson.py`, `qa_gate.py` → تتحول لـ`.com`. تبقى داخلية/مؤقتة على أي مضيف: `piper_tts.py`, `rvc_tts.py`, `/ttslab`, `/private_tmp`.
- **مراحل التنفيذ**: 1) جرد + نسخة احتياطية كاملة (nginx، n8n، `/var/www`، DB). 2) إقامة `.com` بالتوازي (DNS، vhosts، شهادة، كود، webhook تليجرام جديد)، اختبار كل مسار قبل التحويل. 3) 301 من `.online` إلى `.com`، مراقبة 48 ساعة. 4) المستخدم يغيّر حجم الـVPS، Claude يضبط ويختبر. تراجع: الإعدادات القديمة محفوظة، رجوع في ~دقيقة. صفر توقف (`.online` يفضل شغّال طول الوقت).

---

## القسم ٩ — الخطوات القادمة (حالة آخر تحديث)

1. متابعة إنتاج مسار الممارسين (325 درس متبقّي بعد أول درسين، بصوت Cartesia، اعتماد عبر بطاقات تليجرام).
2. خطوة تلخيص تلقائية لمسار الجمهور — بعد تقدّم كافٍ في مسار الممارسين.
3. تقليل الاعتماد البشري تدريجيًا بعد ثبات صفر أخطاء (قرار المستخدم صراحة عند الاستقرار).
4. أتمتات تشغيلية منفصلة لاحقًا (مشاريع مستقلة، ليست جزءًا من مصنع الفيديوهات): رد تلقائي على استفسارات المشتركين، إرسال إيميلات، تطبيق موبايل، فصل مسارات الاشتراك.
5. صور موقع `index.html`: مهمة سابقة مفتوحة كانت مصفوفات الصور بحاجة لإعادة ربط بالأصول الصحيحة (`a-*`/`e-*`/`p-*`) — تحقّق من حالتها الحالية قبل افتراض أنها ما زالت مشكلة.

---

*هذا الملف يُحدَّث يدويًا كلما طرأ قرار أو تغيير جوهري في جلسة عمل. لا يوجد آلية تقنية مضمونة تُشغِّل تحديثًا تلقائيًا عند تعطّل الجلسة فجأة أو انتهائها بلا تدخّل — لذلك يُحدَّث بأقصى تكرار ممكن أثناء العمل نفسه، لا فقط في النهاية.*
